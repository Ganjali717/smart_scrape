# SmartScrape — Environment Variables Template
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# FitLayout API credentials
FITLAYOUT_API_URL=https://layout.fit.vutbr.cz/api
FITLAYOUT_REPO_NAME=IE AI
FITLAYOUT_TOKEN=Bearer ------------

# Optional: override service IDs if they change
# FITLAYOUT_RENDER_SERVICE=FitLayout.Puppeteer
# FITLAYOUT_SEGMENTATION_SERVICE=FitLayout.VIPS

# Path to trained GNN model weights (generated by train.py)
SMARTSCRAPE_MODEL_PATH=model.pt
